fastapi==0.115.2
uvicorn[standard]==0.32.0
httpx==0.27.2
asyncpg==0.30.0
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
pydantic==2.9.2
passlib[bcrypt]==1.7.4
bcrypt<4.0.0  # Pin to pre-4.x to avoid missing __about__ attribute used by passlib 1.7.4
fastapi-cache2==0.2.2
redis==5.0.8
orjson==3.10.18
slowapi==0.1.9


# Pin setuptools to avoid pkg_resources deprecation warning (see https://setuptools.pypa.io)
setuptools<81

# Notes:
# - Using uvicorn[standard] for recommended extras (watchfiles, etc.).
# - Added python-jose[cryptography] to ensure cryptographic backend is available.
# - Removed duplicate lines and fixed malformed concatenation.
# - Added slowapi for rate limiting favourites endpoints (memory backend in dev).
